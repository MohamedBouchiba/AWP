"""Dashboard UI layer: `components` (small render helpers) + `pages` (full pages).

The dashboard CSS lives in assets/css/dashboard.css and is inlined by pages.py.
Edit that .css file to restyle the dashboard — no Python changes needed.
"""
